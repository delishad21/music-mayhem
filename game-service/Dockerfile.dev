FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./
COPY tsconfig.json ./

# Install dependencies
RUN npm install

# Expose port
EXPOSE 5000

# Install deps if needed and use tsx watch for hot reload
CMD ["sh", "-c", "npm install && npm run dev"]
