version: '3.8'

services:
  # MongoDB Database
  mongodb:
    image: mongo:7
    container_name: music-game-mongo
    restart: unless-stopped
    env_file:
      - ./.env
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_INITDB_ROOT_USERNAME}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_INITDB_ROOT_PASSWORD}
      MONGO_INITDB_DATABASE: ${MONGO_INITDB_DATABASE}
    volumes:
      - mongodb_data:/data/db
    networks:
      - music-game-network
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/test --quiet
      interval: 10s
      timeout: 5s
      retries: 5

  # Song Download Service (Python Flask API)
  song-service:
    image: ${DOCKERHUB_USERNAME:-musicgame}/musicgame-song-service:latest
    container_name: music-game-songs
    restart: unless-stopped
    env_file:
      - ./.env
    environment:
      - MONGODB_URI=${MONGODB_URI}
      - SPOTIFY_MARKET=${SPOTIFY_MARKET}
    volumes:
      - song_data:/app/songs
    networks:
      - music-game-network
    depends_on:
      mongodb:
        condition: service_healthy

  # Game API (Express + Socket.IO)
  game-service:
    image: ${DOCKERHUB_USERNAME:-musicgame}/musicgame-game-service:latest
    container_name: music-game-game-service
    restart: unless-stopped
    env_file:
      - ./.env
    environment:
      - PORT=${PORT}
      - MONGODB_URI=${MONGODB_URI}
      - JWT_SECRET=${JWT_SECRET}
      - NODE_ENV=${NODE_ENV}
      - FRONTEND_URL=${FRONTEND_URL}
      - SONG_SERVICE_URL=${SONG_SERVICE_URL}
      - SONG_SERVICE_PATH=${SONG_SERVICE_PATH}
    volumes:
      - song_data:/app/songs:ro
    networks:
      - music-game-network
    depends_on:
      mongodb:
        condition: service_healthy
      song-service:
        condition: service_started

  # Frontend (Next.js)
  frontend:
    image: ${DOCKERHUB_USERNAME:-musicgame}/musicgame-frontend:latest
    container_name: music-game-frontend
    restart: unless-stopped
    env_file:
      - ./.env
    environment:
      - PORT=3000
      - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL}
      - NEXT_BACKEND_URL=${NEXT_BACKEND_URL}
      - NEXTAUTH_URL=${NEXTAUTH_URL}
      - NEXTAUTH_SECRET=${NEXTAUTH_SECRET}
    networks:
      - music-game-network
    depends_on:
      - game-service

  # Single-URL reverse proxy (for production)
  edge:
    image: nginx:1.27-alpine
    container_name: music-game-edge
    restart: unless-stopped
    ports:
      - "5762:80"
    volumes:
      - ./infra/nginx/dev-ngrok.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - music-game-network
    depends_on:
      - frontend
      - game-service

volumes:
  mongodb_data:
    driver: local
  song_data:
    driver: local

networks:
  music-game-network:
    driver: bridge
